1. https://www.sketch.com/s/0reQj/a/d45xMl


2. Deploy to Firebase Hosting
    2.0. npm install -g firebase-tools
    2.1. npm run build
    2.2. firebase login
    2.3. firebase init
    2.4. firebase deploy

3. Quick setup — if you’ve done this kind of thing before
   or
   HTTPS
   SSH
   https://github.com/Talnax/rep-20.git
   Get started by creating a new file or uploading an existing file. We recommend every repository include a README, LICENSE, and .gitignore.

   …or create a new repository on the command line
   echo "# rep-20" >> README.md
   git init
   git add README.md
   git commit -m "first commit"
   git remote add origin https://github.com/Talnax/rep-20.git
   git push -u origin master

   …or push an existing repository from the command line
   git remote add origin https://github.com/Talnax/rep-20.git
   git push -u origin master
   …or import code from another repository
   You can initialize this repository with code from a Subversion, Mercurial, or TFS project.

4.